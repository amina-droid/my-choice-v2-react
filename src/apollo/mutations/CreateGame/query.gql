mutation CreateGame ($name: String!, $observerMode: Boolean, $tournament: ObjectId) {
    createGame(createGameInput: {
        name: $name,
        observerMode: $observerMode,
        tournament: $tournament,
    }) {
        _id,
    }
}
